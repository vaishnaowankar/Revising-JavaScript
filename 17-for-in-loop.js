let fruits = ["apple", "orange", "banana", "mango"];

// It will output the index of the array on by one.
for (let index in fruits) {
  console.log(index);
  // console.log(fruits[index]);
  // Use above statement to print values if using "in loop"
}
