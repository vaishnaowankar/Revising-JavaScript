const navItems = document.getElementsByClassName("nav-item");
console.log(navItems);
// It returns HTML collections which are array like objects
// we can use indexing to get the specific elements.

// The querySelectorALl property can also be used to do the work but it stores the output as nodeList. It is also an array like object but has different usecases.
